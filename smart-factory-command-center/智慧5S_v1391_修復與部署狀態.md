# 智慧5S 1.3.9.1 修復與部署狀態

更新紀錄：最新凌晨 12:53 診斷已收到。編輯器回傳直接 JSONP，正式 GET 成功且主庫吻合；正式 JSONP 為 HTTP 200、JavaScript MIME，帶前導註解與 typeof 保護。原驗收字首判斷無法識別此格式。已修正驗收工具至 1.3.9.5，只需沿用舊批次執行第二階段，不改正式入口或再次部署；正式資料內容與手機 56 筆尚待驗收。

## 已完成

- 核對中央主庫仍有 5S 巡檢主檔、巡檢明細、改善單及存檔索引，並讀取正式表頭。
- 新增及更新回傳可核對的收據，包含協定、主庫、分頁、動作、列號及主鍵。
- 新增以分頁主鍵防重：相同內容重送回傳原列號；相同主鍵但不同內容停止寫入。
- 更新以主鍵定位，避免過時列號覆寫別筆；保留未指定欄位與公式。
- 非 5S 的共用動作及 LINE events 交回既有主路由。
- 手機先保存工作再送出；錯誤、不完整回覆或不符收據皆保留原工作。
- 本機資料庫名稱「智慧5S本機資料庫」、版本 1、資料表「待同步佇列」及識別碼保持一致。
- 離線服務更新保留舊快取，並先取本版程式資源。

## 驗證證據與限制

`node --test tests/智慧5S同步驗證.cjs`：15 項通過、0 項失敗。

測試涵蓋新增／更新／重送、主鍵衝突、錯庫與錯欄拒收、公式保留、錯誤收據保留佇列、伺服器寫入後回覆遺失、部分成功及連點同步。

56 筆合成舊佇列模擬結果為「成功 56／失敗 0／剩餘 0」。這是記憶體替身測試，**不是原手機的 56 筆實際結果**，也不代表已通過 Apps Script、Safari 或正式 HTTP 驗收。

目前沒有手機資料刪除或 LINE 通知的執行證據；正式 HTTP 驗收的後續失敗與獨立讀回結果見下方最新紀錄。雲端瀏覽器登入受限後，改由使用者在自己的瀏覽器套用修復；使用者已提供第 89 版部署成功畫面。前端修改仍保存在修復分支，後端驗收通過前不合併至 main。

## 2026-09-10 接續檢查紀錄

- 曾成功登入正確的 NEXUS OS 編輯器；當時現行主路由為 v1.9.4，既有部署最後確認為第 86 版。
- 已讀取並備份當時的完整主路由，與倉庫 v1.9.5 比對，差異為版本註解及 5S 接收器接點；原 LINE／V4 處理內容相同。
- 已檢查當時 `GAS 後端入口.gs` 的 POST 路徑：LINE 事件使用原有 LINE 權限流程；一般 API 直接進入共用路由。5S 接收器遇到 LINE 事件即交回原路由，不略過 LINE 權限檢查。原 GET 已提供共用讀取及 JSONP 輸出。
- 編輯器中曾新增兩個修復／驗收檔案並嘗試儲存，但檔案標籤與雲端儲存圖示狀態不一致。複製主路由時剪貼簿仍是前一個檔案，故比對保護已停止覆寫；不能把剪貼簿內容一致當作雲端存檔驗證。
- 該階段尚未將正式主路由改為 v1.9.5，尚未更新部署、執行正式驗收或合併前端；後續手動更新結果見下節。
- 操作曾因額度限制中斷；恢復後雲端瀏覽器已重建，登入狀態未保留。Google 登入畫面重試後仍回傳 `502 Bad Gateway / Connection refused`。
- 隨後登入重試被自動審核拒絕，理由是登入已反覆發生通用錯誤，必須停止重試。不得改用其他網址、底層瀏覽器介面或間接方式繞過此限制。使用者已給予的修復授權不需重問，後續由使用者在自己的瀏覽器操作。
- 修復仍保存在草稿 PR #1（`codex/smart5s-1391-receipts`）。原手機 56 筆尚未讀取或同步，不能宣告成功 56／失敗 0／剩餘 0。

## 使用者手動更新的證據

- IMG_6975：主路由程式開頭為 v1.9.5，包含 `智慧5S_POST通用接收_` 接點，畫面顯示已儲存至雲端硬碟。
- IMG_6976／IMG_6977：5S 寫入修復版 1.3.9.1、收據協定、正式主庫及驗收程式的可見內容符合已準備的修復碼。截圖未涵蓋所有行，不等於完整檔案逐位元驗證。
- IMG_6978：管理部署作業選取既有 LINE 正式接線，選擇建立新版本；描述顯示 LINE正式接線v1.9.33。
- IMG_6979：明確顯示「已成功更新部署作業」，89 版（2026年9月11日凌晨2:10）。部署 ID／網址可見前綴與指定的既有 NEXUS 部署一致；完整 ID 在畫面中被截斷，正式驗收函式將以指定完整網址驗證。
- 目前應直接進行既有第 89 版的正式 HTTP 驗收，沒有必要重複部署。首次驗收結果見下節；尚未發布前端或處理原手機 56 筆資料。

## 已授權的正式目的地

- 原手機入口保持 `https://qhero70.github.io/smart-factory-worker-app/5s/`。
- 使用者已同意把 5S 後端改接既有 NEXUS OS 部署，無須重問此項同意。
- Apps Script 專案：`1P4tj0j_U15yzjuCsvqK2ONLBbL0-cEQvMT8URv3XhIlbrnuogqbKYlbo`。
- 更新既有部署：`AKfycby2ghuwkxTr1kbt2bU9D3U24O55c6GhcabA1IhDC67OEw86pH6MjS3nnBMASnjEmggw`。
- 後端網址：`https://script.google.com/macros/s/AKfycby2ghuwkxTr1kbt2bU9D3U24O55c6GhcabA1IhDC67OEw86pH6MjS3nnBMASnjEmggw/exec`。
- 唯一正式主庫：`19osmTlQQ9obDmVvmv5uphFHRwCtd2pkFhe6p3pYMSn8`。
- 保留既有執行身分與存取權限；不新增 Web App、Bot 或另一份試算表。

## 尚待執行

1. 用工具修訂 1.3.9.5 的完整內容覆蓋原 `智慧5S_正式部署驗收_v1391.gs`，不是新增第二份同名函式；不改主入口、正式部署或網址。
2. 使用同一登入帳號，只執行 `驗證_智慧5S正式讀回_v1391`，重用工具 1.3.9.2 已通過的第一階段批次。無須再跑診斷或第一階段，不新增驗收列、不執行 `doPost` 或初始化函式。
3. 核對第二階段的正式主庫資料、一般 GET、JSONP 與驗收主鍵唯一性。新版只接受直接回呼及嚴格的同名 typeof 函式存在保護；其他格式仍停止。實際回覆尚未經新版解析與逐值驗收前，不能標示後端已全部通過。
4. 後端驗收通過後，才合併手機端設定、資料層及離線服務至 main，確認原入口顯示 1.3.9.1。不得清除瀏覽資料、重建 IndexedDB 或重新安裝 PWA。
5. 在原手機原入口執行既有同步操作，核對實際成功／失敗／剩餘數，以及主庫對應主鍵。此工作環境無法直接讀取原手機的 IndexedDB；若仍需手機操作，只說明必要的一步。

正式 POST、LINE 空事件、V4 唯讀、獨立主庫讀回與一般 GET 已有使用者執行記錄證據；JSONP 與原手機 56 筆仍未通過，不能標記全部完成。後續實際部署版本號仍未核實。

## API 參考

- [Google TextFinder：完整儲存格比對及 findAll](https://developers.google.com/apps-script/reference/spreadsheet/text-finder)
- [Google Content Service：Web App 回傳資料](https://developers.google.com/apps-script/guides/content)
- [Google UrlFetchApp：HTTP 驗收與重新導向](https://developers.google.com/apps-script/reference/url-fetch/url-fetch-app)

## 最新：正式讀回差異與兩階段驗收修訂 1.3.9.2

- IMG_6980：首次正式驗收於第 59 行「正式主庫讀回不符」停止。該行之前的新增、重送、更新、衝突及錯库拒收收據斷言已完成；後面的 LINE 空事件與 V4 檢查尚未執行。
- 經獨立 Sheets 讀取 `5S_連線驗收!A1:D10`，唯一資料列為 `驗收-4a720637-3ed9-47a1-88a9-5bc934e5db08`，狀態「更新驗收通過」，備註「合成連線驗收資料，非正式巡檢」，時間 `2026-09-10T18:14:43.851Z`。這確認實際更新正確，但不能推論所有正式驗收已通過。
- 推測原驗收重用 HTTP 寫入前開啟的試算表讀取快取；原因尚未完全證實。不放寬原有主鍵、狀態、備註及時間斷言。
- 完整替換驗收工具：先執行 `驗證_智慧5S正式部署_v1391`，結束後另行執行 `驗證_智慧5S正式讀回_v1391`。第二步使用新的執行程序讀回，並驗證既有 GET 和 JSONP。每次第一步僅新增一個合成驗收主鍵；保留歷史驗收列。
- 此次只更新驗收工具，正式第 89 版不需重新部署；前端仍未發布，手機原始資料未動。
- 本機回歸：`node --test tests/智慧5S驗收分階段驗證.cjs tests/智慧5S同步驗證.cjs` 共 20 項通過。新增測試使用實際後端函式和每次執行的讀取快照，驗證分開執行成功、同次讀回拒絕、錯值和重複主鍵拒絕、缺少前階段證據拒絕、錯誤 JSONP 拒絕。
- 待使用者套用完整驗收工具並提供兩階段執行結果。正式環境與原手機 56 筆仍未完成驗收。

## 最新：依完整入口修正 5S 讀取主庫

- IMG_6987：本批 `驗收-08d6f6ca-cd11-45a0-8716-64ee30c438b9` 的新增、防重、更新、更新重送、衝突拒收、錯庫拒收、LINE 空事件、V4 唯讀、正式讀回與主鍵唯一均為 true。僅在正式 GET 階段未找到唯一主鍵；JSONP 尚未執行。
- 使用者兩份完整 `GAS 後端入口` 附件相同，來源 SHA256 `53ffbe2f8fa07ef6f897f73c3cb68630fba29d2ff7d5b397bf78f193ddfc2328`。原碼共 3940 行，120 個全域函式。
- 原碼 `主檔_讀取分頁資料` 透過舊共用設定開啟 `1RCT…Foq0`，忽略讀取參數中的正式主庫編號；5S 寫入則固定 `19osm…YMSn8`。本機已重現指定正式庫卻開啟舊庫、回覆成功但零列的情況。這是來源中可確認的缺陷；正式修復結果仍待部署驗收。
- 修正版只改動 `主檔_讀取分頁資料`：所有 `5S_` 分頁固定與寫入同一正式庫；有傳入主庫或分頁別名時必須一致；錯庫或參數衝突停止讀取。回覆加入主庫與讀取版本 1.3.9.3，沿用既有資料物件格式。
- 已逐一比對 120 個原函式，只有上述讀取函式改動；原 doGet、JSONP 接點、doPost_舊版備份及其他函式內容相同。未修改或執行通知與初始化。
- 完整修正版存於 `01_GAS後端/GAS_後端入口.gs`，對應使用者編輯器的 `GAS 後端入口.gs`。不是新增另一個入口；套用時覆蓋原檔。
- 本機共 25 項測試通過，包括兩個同名分頁的資料庫選擇、缺表／空表不退回舊庫、錯庫／分頁衝突拒絕，並將兩階段驗收的 GET 改成呼叫完整正式入口程式。JSONP 輸出在本機仍為協議替身，不能當成正式 JSONP 通過。
- 此次修改正式讀取入口，需要更新原部署後再跑第二階段。前端仍在草稿 PR，手機 56 筆未讀取或同步。
- API 依據：[Google SpreadsheetApp.openById](https://developers.google.com/apps-script/reference/spreadsheet/spreadsheet-app#openById(String))。

## 最新：JSONP 回呼輸出修復 1.3.9.4

- IMG_7012／7013：第一階段完整通過。本批主鍵 `驗收-2da53d22-3db4-4e5f-8a09-edbf3ac55374`，收據列號 5；工具 1.3.9.2、寫入 1.3.9.1。
- IMG_7014～7017：第二階段在 2026-09-11 下午 2:33:15 於第 227 行停止，錯誤「JSONP 未回傳指定回呼」。記錄中正式讀回、主鍵唯一及 GET讀取均為 true。舊主庫讀取問題已在本批 GET 驗收中消除。
- 尚未取得此次 JSONP HTTP 回覆全文，不能斷言舊輸出函式的內部錯誤或網路原因。倉庫的相容層函式名稱與正式入口引用不同，故不直接覆蓋未知的現行相容層。
- 主入口 1.3.9.4：僅將既有 `api=讀取分頁資料`、`5S_` 分頁的 doGet 結果交給同檔 `主檔_智慧5S讀取輸出_`。有合法回呼時精確產生 `callback(JSON);` 並設定 JavaScript MIME；無回呼時保留 JSON。禁止非法／衝突回呼，保留失敗旗標及原始字串。
- POST、LINE 及其他 API 輸出路徑未變，主庫讀取修復 1.3.9.3 仍保留；不刪除或初始化手機與試算表資料。
- 本機 31 項測試通過。本次將兩階段測試改為實際呼叫完整 doGet 與正式輸出函式，另驗證原 PWA 的 fetch 失敗後可透過動態回呼讀回資料，且腳本／回呼會清理。試算表、ContentService 及瀏覽器 DOM 仍為本機替身，正式 JSONP／Safari 仍待驗收。
- 下一步：用完整 1.3.9.4 覆蓋使用者目前已套用 1.3.9.3 的同一主入口檔；編輯原部署建立新版本，沿用原網址。保留既有驗收工具，僅執行 `驗證_智慧5S正式讀回_v1391`，無須重跑第一階段或新增驗收列。
- 原第 89 版之後的具體部署版本號尚未由新版管理部署畫面確認，不臆測版本號。前端仍在草稿 PR，未合併至 main。
- [Google Content Service 的 JSONP 輸出方式](https://developers.google.com/apps-script/guides/content#serving_jsonp_in_web_pages)

## 最新：IMG_7090 與唯讀回呼診斷

- 最新截圖的錯誤仍在 `智慧5S_正式部署驗收_v1391.gs:227`，由第二階段第 181 行 JSONP 呼叫引發。畫面中的工具版本 1.3.9.2 是預期的驗收工具版本，不能拿它當作正式主入口版本。
- 截圖未提供 JSONP 回應原文或最新管理部署版本；也不能由左側截斷的 `GAS_後端入口_完…` 檔名判定是否有重複 doGet。可能原因需比較實際入口與回覆後再確認。
- 新增 `01_GAS後端/智慧5S_正式回呼唯讀診斷_v1.gs`，公開手動函式 `診斷_智慧5S正式回呼`。不定義／改寫 doGet、doPost，不更動原有驗收工具或主入口。
- 工具以相同讀取 API、固定正式主庫與 `5S_連線驗收` 分頁、10 筆上限，執行一次編輯器 doGet 與各一次正式 GET／JSONP；不 POST、不重試、不寫入試算表或屬性、不建立觸發器、不通知、不接觸手機。
- 記錄僅包含目前函式接點、HTTP 狀態、安全 MIME、回覆字元數、JSON／JSONP／HTML 分類、回呼名稱與非敏感摘要。不輸出資料列、任意標頭、回應原文、Cookie、權杖或重新導向網址；回覆僅 JSON.parse，不 eval。
- 前導註解、空白、不同回呼、HTML 登入／權限／錯誤頁及連線例外分開記錄；若一條路徑失敗，仍記錄另一條路徑。診斷結果永遠註明「不代表正式驗收通過」，不放寬既有 JSONP 驗收。
- 本機診斷測試 13 項通過，連同既有回歸共 44 項。使用合成資料與服務替身；不等於正式 Google／Safari 通過。
- 雲端登入與直接開啟正式網址仍受限，不重試或改用其他網址繞過。只需使用者在已登入的編輯器執行唯讀診斷並提供四行記錄；本次不要求新部署或再次執行第一階段。
- API 依據：[HTTPResponse](https://developers.google.com/apps-script/reference/url-fetch/http-response)、[TextOutput](https://developers.google.com/apps-script/reference/content/text-output)。

## 最新：依凌晨 12:53 診斷修正驗收工具至 1.3.9.5

- 使用者提供完整四行診斷：編輯器有 doGet、1394 輸出函式與接點，回覆 631 字元，直接 JSONP、指定回呼相符、主庫吻合、讀取版 1.3.9.3、4 筆。
- 正式 GET：HTTP 200、application/json、609 字元、主庫吻合、讀取版 1.3.9.3、4 筆。這 4 筆來自 `5S_連線驗收`，不是原手機的 56 筆。
- 正式 JSONP：HTTP 200、text/javascript、680 字元、1 段前導註解、含指定回呼及開頭 typeof。診斷 1.0.0 的「其他文字」只是分類器未支援該外框，不能當成 JavaScript 必然無效或資料遺失的證據。
- 以 `/**/typeof smart5s_verify_1391 === 'function' && smart5s_verify_1391(JSON);` 模擬，會比直接回呼增加 49 字元，與 680−631 相同；這與診斷線索一致，但尚未取得逐字正式本文，故不把模擬當成正式全文證明。
- 原驗收只接受字首為 `smart5s_verify_1391(`，因此確實無法辨識帶註解／保護條件的合法寫法。編輯器和正式輸出格式不同的原因及實際部署版號仍未判定，不據此要求重新部署。
- 工具 1.3.9.5 的 `智慧5S驗收解析JSONP_` 僅剝除前導註解並識別兩種明確外框：直接回呼；或 `typeof 同名回呼 === 'function' && 同名回呼(JSON)`。只用 JSON.parse，保護條件與呼叫名稱須一致，額外指令、錯誤條件、其他回呼或非 JSON 參數均拒絕。完整支援 JavaScript 四種行終止符，避免行註解誤吞指令。
- 新版第二階段接受工具 1.3.9.2 的既有通過批次，但仍要求同一主庫／分頁、有效階段及全部 8 項第一階段檢查。其他未知版本與缺少證據的批次拒絕。成功後記錄讀回工具版本與回呼外框；不需要再次 POST 或增加驗收資料。
- 原主鍵唯一、列號、狀態、備註、時間與失敗旗標檢查保留；回覆明示主庫編號時另外核對一致。手機佇列、正式主入口、POST 路由與前端原始碼均未改動。
- 全部 55 項本機測試通過，含原 PWA 的直接／受保護回呼與清理流程、舊批次沿用、錯值／錯庫／重複主鍵拒絕、額外程式碼拒絕。服務與瀏覽器仍為本機替身，不是正式 Google／原手機同步結果。
- 下一步僅替換原驗收工具並執行 `驗證_智慧5S正式讀回_v1391`，以真實讀回結果判定後端是否通過。前端修復分支仍未合併，原手機 56 筆未讀取、未同步、未清除。
