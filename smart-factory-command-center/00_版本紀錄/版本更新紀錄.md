# 版本更新紀錄

## v1.0.0｜正式主線建立

完成內容：

- 建立 `smart-factory-command-center/` 正式專案目錄。
- 建立 `docs/` GitHub Pages 入口。
- 建立 GAS 後端。
- 建立 HTML5 PWA 前端。
- 建立 LINE Bot 單一入口設定。
- 建立部署說明、測試資料、欄位規格、驗收清單。

## v1.0.1｜GitHub 主線清理

完成內容：

- 移除舊 Node / Expo / PostgreSQL / Redis / apps monorepo 架構。
- 根目錄只保留正式版 README、docs、smart-factory-command-center。
- 專案定位改為 Google Sheets + GAS + HTML5 PWA + LINE Bot。

## v1.1.0｜系統維護與部署穩定化

完成內容：

- 新增 `.nojekyll`，避免 GitHub Pages 被 Jekyll 干擾。
- 新增 Apps Script 專案設定 `appsscript.json`。
- 新增 `系統維護工具.gs`。
- 支援一鍵自檢。
- 支援欄位修復。
- 支援建立 Google Sheets 備份。
- 支援 CSV 文字匯入。
- 支援每日戰情快照。
- 新增營運儀表板 KPI 規格。

## v1.2.0｜前端戰情室強化與維護工具接入

完成內容：

- GAS 主後端版本升級為 `正式完整版_v1.2.0`。
- 新增 API：`取得KPI戰情`。
- 新增 API：`系統自檢`。
- 新增 API：`修復欄位`。
- 新增 API：`建立備份`。
- 新增 API：`每日快照`。
- 新增 API：`匯入測試資料`。
- LINE Bot 戰情回覆增加達成率、不良率、停機分鐘、逾期警示。
- GitHub Pages 前端 `docs/app.html` 新增 KPI 頁。
- GitHub Pages 前端新增維護頁。
- 新增工單達成率圖表。
- 新增停機排行。
- 新增不良排行。
- 新增一鍵初始化、一鍵自檢、修復欄位、建立備份、每日快照、匯入測試資料按鈕。
- 同步更新正式前端原始碼 `02_HTML前端/index.html`。

## v1.3.0｜正式資料管理與資料治理

完成內容：

- GAS 主後端版本升級為 `正式完整版_v1.3.0`。
- 新增 API：`取得主檔管理資料`。
- 新增 API：`新增或更新主檔`。
- 新增 API：`停用主檔資料`。
- 新增 API：`匯入CSV主檔`。
- 支援管理：人員主檔、產品主檔、機台主檔、工站主檔、工單主檔、檢具主檔。
- 新增 GitHub Pages 資料管理頁：`docs/data.html`。
- 資料管理頁支援查看資料、編輯單筆、停用資料、下載 CSV 模板、貼上 CSV 匯入。
- GitHub Pages 首頁新增資料管理入口。
- 新增正式資料 CSV 模板資料夾：`06_正式資料模板`。
- 新增人員、產品、機台、工站、工單、檢具 CSV 模板。
- 新增 `資料治理規則.md`，固定主鍵、停用、產品分類、機台狀態、工單狀態與 CSV 匯入規則。

## v1.4.0｜生產計畫清洗、工單生成、排程需求池

完成內容：

- 新增 GitHub Pages 計畫清洗頁：`docs/plan.html`。
- 新增流程：標準計畫 CSV → 清洗檢查 → 產生工單 → 寫入工單主檔。
- 新增排程需求池 CSV 產出功能。
- 新增生產計畫標準匯入模板：`06_正式資料模板/生產計畫標準匯入模板.csv`。
- 新增排程系統規格文件：`10_排程系統/生產計畫清洗規格.md`。
- GitHub Pages 首頁新增「計畫清洗」入口。
- 此版先支援標準化 CSV，不直接解析複雜合併儲存格 Excel。

## v1.5.0｜排程需求池直接寫入與主檔缺漏檢查

完成內容：

- GAS 主後端版本升級為 `正式完整版_v1.5.0`。
- 將 `10_排程需求池` 納入正式主檔管理。
- 新增 API：`寫入排程需求池`。
- 新增 API：`檢查計畫主檔缺漏`。
- 新增 API：`檢查主檔完整性`。
- 計畫清洗頁 `docs/plan.html` 升級為 v1.5.0。
- 計畫清洗頁新增「檢查缺漏」。
- 計畫清洗頁新增「寫入需求池」。
- 計畫清洗頁新增「忽略缺漏強制寫入需求池」。
- 資料管理頁 `docs/data.html` 新增 `10_排程需求池` 管理入口。
- 資料管理頁新增「主檔完整性檢查」按鈕。
- 主檔缺漏檢查支援產品、工站、機台三類資料。

## v1.6.0｜缺漏補齊與主管摘要

完成內容：

- 新增 GitHub Pages 缺漏主檔補齊工具：`docs/repair.html`。
- 缺漏補齊工具可將缺產品轉為 `02_產品主檔` CSV 草稿。
- 缺漏補齊工具可將缺工站轉為 `04_工站主檔` CSV 草稿。
- 缺漏補齊工具可將缺機台轉為 `03_機台主檔` CSV 草稿。
- 新增 GitHub Pages 主管會議摘要工具：`docs/meeting.html`。
- 主管摘要工具可抓取 `取得KPI戰情` 與 `取得AI摘要`。
- 主管摘要工具可產出可複製的會議摘要文字。
- GitHub Pages 首頁新增「缺漏補齊」入口。
- GitHub Pages 首頁新增「主管摘要」入口。

## v1.7.0｜排程需求池狀態流轉與排程看板

完成內容：

- 新增 GitHub Pages 排程需求池看板：`docs/schedule.html`。
- 排程看板可讀取 `10_排程需求池`。
- 排程看板依狀態分欄：待排程、已排程、生產中、完成、暫停。
- 每張需求卡可直接切換狀態。
- 狀態切換會透過 `新增或更新主檔` 寫回 Google Sheets。
- 排程看板可產生排程摘要並複製。
- GitHub Pages 首頁新增「排程看板」入口。
- 新增排程需求池狀態流轉規格：`10_排程系統/排程需求池狀態流轉規格.md`。

## v1.8.0｜交期警示、主管週月報與 LINE 主選單規格

完成內容：

- 排程看板 `docs/schedule.html` 升級為 v1.8.0。
- 排程看板新增逾期紅色警示。
- 排程看板新增 3 天內到期黃色提醒。
- 排程看板新增搜尋功能。
- 排程看板新增排序功能：交期由近到遠、交期由遠到近、需求量大到小、更新時間新到舊。
- 排程看板新增風險篩選：全部、逾期、即將到期、未完成。
- 排程摘要新增高風險需求清單。
- 主管摘要 `docs/meeting.html` 升級為 v1.8.0。
- 主管摘要新增日報 / 週報 / 月報模式。
- 主管摘要新增本期重點欄位。
- 新增 LINE Bot Flex Message 主選單規格：`03_LINEBot/LINEBot_Flex_Message_主選單規格.md`。
- GitHub Pages 首頁更新為 v1.8.0 狀態。

## v1.9.0｜LINE Bot Flex Message 主選單 GAS 實作

完成內容：

- 新增 GAS 擴充檔：`01_GAS後端/LINEBot_FlexMessage主選單.gs`。
- 新增函數：`產生LINE主選單Flex_()`。
- 新增函數：`產生LINE回覆訊息_(文字)`。
- 新增函數：`回覆LINE訊息陣列_(replyToken, messages)`。
- 新增測試函數：`測試_LINE主選單FlexJSON()`。
- 新增測試函數：`測試_LINE文字回覆陣列()`。
- 支援文字指令：選單、主選單、功能、入口。
- 支援文字指令：戰情、狀況、AI摘要、AI。
- 新增 LINE Bot Flex 接線說明：`03_LINEBot/LINEBot_Flex_接線說明.md`。
- GitHub Pages 首頁更新為 v1.9.0 狀態。

## v2.0.0｜正式上線檢查與 LINE Webhook 整併片段

完成內容：

- 新增 GitHub Pages 正式上線檢查頁：`docs/go-live.html`。
- 正式上線檢查頁可檢查 GAS Web App URL、健康檢查、首頁資料、主檔完整性、KPI 戰情。
- 正式上線檢查頁可產生並複製上線檢查報告。
- 新增 GAS 替換片段：`01_GAS後端/LINEWebhook整併版_替換片段.gs`。
- LINE Webhook 整併片段可直接接上 Flex Message 主選單。
- LINE Webhook 整併片段保留 `回覆LINE_()` 與 `產生LINE回覆_()` 舊函數相容。
- 新增部署文件：`04_部署說明/正式上線檢查清單_v2.0.0.md`。
- GitHub Pages 首頁新增「上線檢查」入口並更新為 v2.0.0 狀態。

---

## 下一版 v2.1.0 預計項目

- 排程看板支援負責人、預計開工日、預計完工日。
- 生產計畫清洗支援供料日、出貨日、訂單欠量。
- 主管摘要可合併排程需求池摘要。
- 建立正式資料缺漏匯總頁。
