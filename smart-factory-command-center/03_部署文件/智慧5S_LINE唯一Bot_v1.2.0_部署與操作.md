# 化新精密｜智慧 5S｜唯一 LINE Bot v1.2.0 部署與操作

文件日期：2026-08-11

適用系統：智慧 5S 管理平台 v1.0.2

LINE 模組版本：v1.2.0（群組綁定 v1.1.0＋身分權限 v1.8.0 自包含版）

## 一、正式架構

- 只沿用既有「NEXUS OS 智慧製造系統」LINE Bot。
- 不建立第二個 LINE 官方帳號、不建立第二組 Channel Access Token。
- 既有 LINE Apps Script 專案識別碼：`1P4tj0j_U15yzjuCsvqK2ONLBbL0-cEQvMT8URv3XhIlbrnuogqbKYlbo`。
- 唯一中央資料庫識別碼：`19osmTlQQ9obDmVvmv5uphFHRwCtd2pkFhe6p3pYMSn8`。
- 智慧 5S 正式入口：<https://qhero70.github.io/smart-factory-worker-app/5s/?v=102>。
- PWA 將重大巡檢異常與新增紅牌寫入 `5S_通知紀錄`；既有 LINE Bot 橋接負責推播。

## 二、v1.1.0 新增內容

v1.2.0 追加修正：

- `33_LINE` 強制使用 `19osmTlQQ9obDmVvmv5uphFHRwCtd2pkFhe6p3pYMSn8`，不再依賴舊的 `取得試算表_()` 或 `讀表_()`。
- 同步鎖定 `智慧製造_SPREADSHEET_ID` 與 `智慧製造中央作戰資料庫_ID`。
- 第一次執行自動建立 `33_LINE身份權限`、`33_LINE權限紀錄`。
- LINE 回覆直接檢查 HTTP 狀態碼，失敗不再靜默略過。
- `解除綁定` 同步清除 `01_人員主檔.LINE_USER_ID`，避免身分被自動重建。

1. 群組內可直接輸入指令，自動讀取 Webhook 的真實 LINE 群組識別碼。
2. 只有已完成 LINE 身分綁定，且具主管、班長、工程師或更高權限者可以設定。
3. 綁定前呼叫 LINE 群組摘要 API，確認既有 Bot 確實位於該群組。
4. 已綁定其他群組時拒絕普通覆蓋，必須使用「改綁確認」。
5. 解除綁定必須使用「解除確認」，且只能解除目前群組擁有的區域。
6. 自動建立 `5S_LINE群組綁定紀錄`，保存操作者、區域、群組與異動結果。
7. 通知自動加入通知編號與智慧 5S PWA 入口。
8. 依 `去重鍵` 阻止已發送案件再次推播。
9. 所有啟用區域未完成群組設定時，拒絕建立每小時觸發器。
10. 偵測到主 GAS 的 `智慧5S_發送待通知自動化` 觸發器時，拒絕建立第二條通知路徑。
11. 受控測試必須輸入固定確認文字，避免管理員誤觸推播。

## 三、部署檔案

本次只需同步下列兩個完整檔案到既有 NEXUS OS Apps Script 專案：

1. `38_7_正式完整主檔資料庫ID設定.gs`
2. `33_LINE_主管權限與身份綁定.gs`

下列既有檔案保持原內容，不需重貼：

- `智慧5S_LINE群組綁定.gs`
- `智慧5S_LINE唯一Bot橋接.gs`
- `GAS 後端入口.gs` 的 `doPost(e)`

現行 `doPost(e)` 已是「5S 群組指令 → LINE 身分權限 → 原有 NEXUS OS API」，本次不得新增第二個入口函式。

## 四、雲端發布順序

1. 確認既有 `LINE_CHANNEL_ACCESS_TOKEN` Script Property 存在；不得讀出、複製或寫入 Git。
2. 完整覆蓋上述兩個本次更新檔案並儲存。
3. 執行 `套用33_LINE唯一正式主資料庫設定()`。
4. 執行 `驗收33_LINE正式資料庫設定()`，結果必須為 `成功: true`。
5. 確認指定試算表已建立 `33_LINE身份權限` 與 `33_LINE權限紀錄`。
6. 更新既有 Web App 部署，保留部署 ID、原網址與 LINE Webhook URL；不得建立第二個部署入口。

## 五、現場第一次啟用

第一階段建議四區共用一個正式群組：`A9 智慧5S示範區`。

1. 由 LINE 群組管理員建立或指定正式群組。
2. 將既有 NEXUS OS LINE Bot 邀請進群組。
3. 操作者先在 Bot 一對一聊天室輸入：

```text
綁定 你的工號
```

4. 回到正式群組輸入：

```text
5S群組說明
```

5. 查詢目前四區狀態：

```text
5S群組狀態
```

6. 四個啟用區域共用目前群組：

```text
5S群組綁定 全部
```

7. 再次輸入 `5S群組狀態`，四區都應顯示「已綁定本群組」。
8. 到中央資料庫確認 `5S_區域主檔.LINE群組識別碼` 四列已寫入，並確認 `5S_LINE群組綁定紀錄` 有四筆完成紀錄。

## 六、改綁與解除

普通綁定發現其他群組時只會提示，不會覆蓋。經區域主管與 LINE 管理員確認後才輸入：

```text
5S群組改綁確認 區域代碼
```

四區全部改綁：

```text
5S群組改綁確認 全部
```

解除目前群組的一個區域：

```text
5S群組解除確認 區域代碼
```

解除目前群組擁有的全部區域：

```text
5S群組解除確認 全部
```

解除指令不會動到已屬於其他群組的區域。

## 七、受控推播測試

先執行唯讀預覽：

```javascript
智慧5S_LINE橋接_預覽待通知(5)
```

確認區域與群組正確後，執行一筆受控實際測試：

```javascript
智慧5S_LINE橋接_發送區域測試('A9-1069檢驗桌', '確認發送測試')
```

驗收標準：

- 正式群組只收到一則測試通知。
- 通知包含區域名稱、通知編號與智慧 5S PWA 入口。
- `5S_通知紀錄` 對應測試列狀態為「已發送」。
- 非正式群組沒有收到訊息。
- 若發送中斷，測試列不會由排程自動重送。

## 八、建立正式排程

測試通過後執行：

```javascript
智慧5S_LINE橋接_建立每小時觸發器()
```

函式會先確認：

- 既有 Bot 權杖已設定。
- 既有 `發送LINE通知()` 可用。
- 中央區域與通知分頁可用。
- 每個啟用區域都有群組識別碼。
- 每個正式收件群組都已有一筆成功的受控群組測試紀錄。
- 沒有 `智慧5S_發送待通知自動化` 衝突觸發器。

禁止在主 5S GAS 專案執行 `智慧5S_建立每日觸發器_()`，因該函式會同時建立主 GAS 直接通知路徑，可能造成重複推播。每日統計只保留 `智慧5S_每日自動化`；LINE 通知只保留既有 NEXUS OS 專案的 `智慧5S_LINE橋接_每小時`。

## 九、緊急停止與復原

發現錯群組、重複通知或大量異常時，立即執行：

```javascript
智慧5S_LINE橋接_停止每小時觸發器()
```

保留 `5S_通知紀錄` 與 `5S_LINE群組綁定紀錄`，不要批次改成已發送。修正後依序執行健康檢查、唯讀預覽、單筆受控測試，再恢復每小時排程。

## 十、本機驗收

執行：

```bash
node smart-factory-command-center/08_測試與驗收/智慧5S_LINE橋接_正式驗收.js
```

v1.2.0 封版結果：23 項通過、0 項失敗。測試完全在記憶體執行，不連線、不推播、不修改正式資料。

## 十一、2026-08-11 正式資料唯讀核對

透過 Google Sheets 唯讀介面核對唯一中央資料庫，沒有執行任何寫入：

- `5S_區域主檔` 有四個啟用區域。
- 四區的 `LINE群組識別碼` 均為空白。
- `5S_通知紀錄` 目前只有一筆 v1.0.2 正式上線驗收封存資料。
- `01_人員主檔` 有工號 `fhfi573`、姓名黃嘉欣、製造部、製一組、工程師，`LINE_USER_ID` 尚未填入。
- 尚未建立 `33_LINE身份權限` 與 `33_LINE權限紀錄`，第一次初始化後應自動建立。

## 十二、正式結案條件

- [ ] 資料庫設定與 `33_LINE` v1.8.0 已同步至既有 LINE Apps Script。
- [ ] `驗收33_LINE正式資料庫設定()` 顯示 `成功: true`。
- [ ] Web App 保留原 URL、原部署 ID 並升版完成。
- [ ] 四個 A9 區域已由核准群組完成綁定。
- [ ] 群組狀態、主檔與綁定紀錄三方一致。
- [ ] 單筆受控測試只送到正確群組一次。
- [ ] 健康檢查顯示 `可正式啟用: true`。
- [ ] 只有 `智慧5S_LINE橋接_每小時` 一條 LINE 通知排程。
- [ ] 重大巡檢異常與新增紅牌各完成一筆實機驗收。
