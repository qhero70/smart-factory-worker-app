name: 38.6_手動檢查

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: check files
        run: |
          test -f 'smart-factory-command-center/01_GAS後端/總控_38_6_doPost最終接線.gs'
          test -f 'smart-factory-command-center/03_部署文件/38.6_Git同步GAS主後端_v1.7.5.md'
          echo '38.6 check passed.'
