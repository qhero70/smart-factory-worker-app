# Apps Script 實際同步部署執行檢查

專案：智慧製造中央作戰指揮中心  
階段：第二十六階段  
日期：2026-06-23  
狀態：部署執行前檢查完成，Apps Script 實際部署待執行

---

## 一、階段判定

本階段完成的是「實際同步部署執行檢查」與「可執行包確認」。

目前不能宣稱已部署，原因：

```text
目前工具可操作 GitHub 與 Google Sheets，沒有 Apps Script IDE / clasp 部署權限。
```

正式狀態：

| 項目 | 狀態 |
|---|---|
| GitHub 最新 GAS 檔 | 已確認 |
| Apps Script 同步檔案 | 已確認只需 `LINE身份綁定.gs` |
| Web App 新版本部署 | 待執行 |
| LINE Developers Webhook 測試 | 待執行 |
| LINE 現場綁定測試 | 待執行 |

---

## 二、必須同步的唯一檔案

| GitHub 檔案 | Apps Script 檔案 | 動作 |
|---|---|---|
| `gas/LINE身份綁定.gs` | `LINE身份綁定.gs` | 新增或覆蓋 |

確認該檔案必須含有：

```javascript
function LINE身份權限_嘗試處理Webhook_(payload) {
```

---

## 三、部署前檢查

| 檢查項 | 標準 | 結果 |
|---|---|---|
| Apps Script 左側有 `智慧製造中央作戰指揮中心.gs` | 存在，不覆蓋 | 待執行 |
| Apps Script 左側有 `系統維護工具.gs` | 存在，不動 | 待執行 |
| 新增 `LINE身份綁定.gs` | 貼入 GitHub 最新版 | 待執行 |
| 函數清單可看到 `LINE身份權限_嘗試處理Webhook_` | 必須看到 | 待執行 |
| Script Properties 有 `智慧製造_SPREADSHEET_ID` | 指向正式主庫 | 待執行 |
| Script Properties 有 `LINE_CHANNEL_ACCESS_TOKEN` | 正式 token | 待執行 |

---

## 四、Web App 部署設定

| 設定項 | 正確值 | 結果 |
|---|---|---|
| 部署類型 | 網頁應用程式 | 待執行 |
| 版本 | 新增版本 | 待執行 |
| 說明 | `v38.7_LINE身份綁定正式上線版` | 待執行 |
| 執行身分 | 我 | 待執行 |
| 存取權限 | 任何人 | 待執行 |
| Webhook URL | 維持原 `/exec` | 待執行 |

---

## 五、部署後 LINE 測試

### 測試 1：既有功能不可壞

輸入：

```text
主檔檢查
```

成功標準：仍可回覆主檔筆數。

### 測試 2：綁定

輸入：

```text
綁定 fhfi546
```

成功標準：

```text
綁定成功
工號：fhfi546
姓名：黃俊偉
角色：作業員
你現在可以使用：我的任務
```

### 測試 3：我的任務

輸入：

```text
我的任務
```

成功標準：依 `33_LINE身份權限` 取得工號後查 `20_今日派班`，回覆待報工任務或無任務提示。

### 測試 4：解除綁定

輸入：

```text
解除綁定
```

成功標準：`33_LINE身份權限` H欄 `啟用` 改為 `否`。

---

## 六、部署完成後才可填寫

| 項目 | 結果 |
|---|---|
| Apps Script 已同步 | 待填 |
| Web App 已部署新版本 | 待填 |
| LINE Webhook 驗證 | 待填 |
| 主檔檢查 | 待填 |
| 綁定測試 | 待填 |
| 我的任務 | 待填 |
| 解除綁定 | 待填 |
| 是否正式上線 | 待填 |

---

## 七、目前結論

```text
第二十六階段：部署執行檢查完成。
Apps Script 實際同步與 Web App 新版本部署尚待執行。
LINE 身份綁定尚未正式上線。
```
